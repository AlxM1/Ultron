# =============================================================================
# 00raiser Consolidated Stack — Environment Configuration
# =============================================================================
# 1. cp .env.example .env
# 2. Generate real secrets (see bottom of file)
# 3. chmod 600 .env
# =============================================================================

# =============================================================================
# GLOBAL
# =============================================================================
COMPOSE_PROJECT_NAME=raiser
TZ=UTC

# IP of the Windows machine with RTX GPU (used by whisperflow & newsletter)
WINDOWS_PC_IP=192.168.1.100

# =============================================================================
# POSTGRESQL (shared instance — superuser for init-databases.sh)
# =============================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_postgres_superuser_pw

# =============================================================================
# REDIS (shared instance)
# =============================================================================
REDIS_PASSWORD=changeme_redis_pw

# =============================================================================
# AUTHENTIK (SSO / Identity Provider)
# =============================================================================
AUTHENTIK_SECRET_KEY=changeme_authentik_secret_at_least_50_chars_long_xxxxxxxx
AUTHENTIK_BOOTSTRAP_PASSWORD=changeme_authentik_admin_pw
AUTHENTIK_BOOTSTRAP_EMAIL=admin@example.com
AUTHENTIK_DB_PASSWORD=changeme_authentik_db_pw

# =============================================================================
# AGENTSMITH
# =============================================================================
AGENTSMITH_DB_PASSWORD=changeme_agentsmith_db_pw
AGENTSMITH_JWT_SECRET=changeme_64char_hex_0000000000000000000000000000000000000000
AGENTSMITH_JWT_REFRESH_SECRET=changeme_64char_hex_1111111111111111111111111111111111111111
AGENTSMITH_ENCRYPTION_KEY=changeme_exactly_32characters!
AGENTSMITH_SESSION_SECRET=changeme_64char_hex_2222222222222222222222222222222222222222
AGENTSMITH_LOG_LEVEL=info
AGENTSMITH_WORKER_CONCURRENCY=5

# URLs (update for production domain)
AGENTSMITH_APP_URL=http://localhost:3000
AGENTSMITH_API_URL=http://localhost:4000
AGENTSMITH_CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Optional: SSO providers
AGENTSMITH_GOOGLE_CLIENT_ID=
AGENTSMITH_GOOGLE_CLIENT_SECRET=
AGENTSMITH_GITHUB_CLIENT_ID=
AGENTSMITH_GITHUB_CLIENT_SECRET=

# Optional: SMTP
AGENTSMITH_SMTP_ENABLED=false
AGENTSMITH_SMTP_HOST=
AGENTSMITH_SMTP_PORT=587
AGENTSMITH_SMTP_USER=
AGENTSMITH_SMTP_PASSWORD=
AGENTSMITH_SMTP_FROM=noreply@agentsmith.local

# Optional: AI providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=

# =============================================================================
# KRYA
# =============================================================================
KRYA_DB_PASSWORD=changeme_krya_db_pw
KRYA_AUTH_SECRET=changeme_krya_auth_secret_base64

# OAuth (optional)
KRYA_GOOGLE_CLIENT_ID=
KRYA_GOOGLE_CLIENT_SECRET=
KRYA_GITHUB_CLIENT_ID=
KRYA_GITHUB_CLIENT_SECRET=

# AI providers (at least one required)
FAL_KEY=
REPLICATE_API_TOKEN=
TOGETHER_API_KEY=
KRYA_OPENAI_API_KEY=
KRYA_GOOGLE_AI_API_KEY=
GOOGLE_IMAGE_MODEL=imagen-4.0-generate-preview-05-20
GOOGLE_VIDEO_MODEL=veo-3.0-generate-preview

# Local AI (optional)
COMFYUI_URL=http://127.0.0.1:8188
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2

# Storage (optional — defaults to local)
KRYA_AWS_ACCESS_KEY_ID=
KRYA_AWS_SECRET_ACCESS_KEY=
KRYA_AWS_REGION=us-east-1
KRYA_S3_BUCKET=krya-uploads

# Payments (optional)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# =============================================================================
# VOICEFORGE
# =============================================================================
VOICEFORGE_DB_PASSWORD=changeme_voiceforge_db_pw
VOICEFORGE_SECRET_KEY=changeme_voiceforge_secret_key
VOICEFORGE_DEVICE=cpu
# TTS_MODEL=tts_models/multilingual/multi-dataset/xtts_v2
# WHISPER_MODEL=large-v3

# =============================================================================
# YOUTUBEDL
# =============================================================================
YOUTUBEDL_DB_PASSWORD=changeme_youtubedl_db_pw
YOUTUBEDL_API_KEY=changeme_youtubedl_api_key_token
YOUTUBEDL_MAX_CONCURRENT=3
YOUTUBEDL_EXTRACT_RATE_LIMIT=10
YOUTUBEDL_MIN_FREE_DISK_MB=500

# =============================================================================
# WHISPERFLOW
# =============================================================================
WHISPERFLOW_API_KEY=
# Points to Windows GPU server for faster-whisper transcription
WHISPERFLOW_GPU_SERVER=ws://${WINDOWS_PC_IP}:8765

# =============================================================================
# NEWSLETTER PIPELINE
# =============================================================================
NVP_DB_PASSWORD=changeme_nvp_db_pw
NVP_SECRET_KEY=changeme_nvp_secret_hex_32
NVP_API_KEY=changeme_nvp_api_key

# GPU server ports on Windows PC
NVP_GPU_VOICE_PORT=5002
NVP_GPU_AVATAR_PORT=5003

# LLM for script generation
NVP_LLM_PROVIDER=local
NVP_LLM_MODEL=qwen2.5:7b

# Social publisher credentials (all optional)
NVP_TIKTOK_CLIENT_KEY=
NVP_TIKTOK_CLIENT_SECRET=
NVP_INSTAGRAM_USERNAME=
NVP_INSTAGRAM_PASSWORD=
NVP_X_API_KEY=
NVP_X_API_SECRET=
NVP_X_ACCESS_TOKEN=
NVP_X_ACCESS_TOKEN_SECRET=

# Stock media APIs (optional)
PEXELS_API_KEY=
PIXABAY_API_KEY=

# Monitoring (optional)
NVP_GRAFANA_USER=admin
NVP_GRAFANA_PASSWORD=changeme_grafana_pw

# =============================================================================
# PORTAL
# =============================================================================
# Portal is a placeholder — add env vars here as the app develops

# =============================================================================
# GENERATING SECRETS — run these and paste results above
# =============================================================================
# POSTGRES_PASSWORD:              openssl rand -base64 32
# REDIS_PASSWORD:                 openssl rand -base64 32
# AUTHENTIK_SECRET_KEY:           openssl rand -base64 60
# AUTHENTIK_DB_PASSWORD:          openssl rand -base64 32
# AGENTSMITH_DB_PASSWORD:         openssl rand -base64 32
# AGENTSMITH_JWT_SECRET:          openssl rand -hex 32
# AGENTSMITH_JWT_REFRESH_SECRET:  openssl rand -hex 32
# AGENTSMITH_ENCRYPTION_KEY:      openssl rand -hex 16  (exactly 32 chars)
# AGENTSMITH_SESSION_SECRET:      openssl rand -hex 32
# KRYA_DB_PASSWORD:               openssl rand -base64 32
# KRYA_AUTH_SECRET:               openssl rand -base64 32
# VOICEFORGE_DB_PASSWORD:         openssl rand -base64 32
# VOICEFORGE_SECRET_KEY:          openssl rand -base64 32
# YOUTUBEDL_DB_PASSWORD:          openssl rand -base64 32
# YOUTUBEDL_API_KEY:              python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# NVP_DB_PASSWORD:                openssl rand -base64 32
# NVP_SECRET_KEY:                 openssl rand -hex 32
# NVP_API_KEY:                    openssl rand -base64 32
# NVP_GRAFANA_PASSWORD:           openssl rand -base64 16
